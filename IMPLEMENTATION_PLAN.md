# Lyrico Desktop 重构与功能对齐计划

## 1. 目标与约束

本计划覆盖最初 17 项需求及后续补充的批处理重设计、应用级右键菜单、移动端字段对齐、取消分页和封面预加载优化。实施时遵循以下原则：

1. 音频文件是标签、歌词、封面和 ReplayGain 的最终事实来源；数据库保存可查询的媒体库索引、关系、插件、任务和日志，JSON 保存应用配置与插件 manifest。
2. 桌面端插件协议与移动端 API v3 对齐，复用 `manifest.json + source.js + includeDirs` 包结构和标准字段，不另建不兼容协议。
3. 扫描、插件调用、批处理、封面解析都不得阻塞前端事件循环；长任务必须可观察、可取消、可恢复查看结果。
4. 先建立数据层和任务层边界，再改页面，避免页面直接调用 Tauri command 并维护重复状态。
5. 每个阶段都需要通过前端单元测试、TypeScript 构建、Rust 测试/检查和对应的人工验收用例。

## 2. 需求分组与依赖关系

### A. 稳定性与性能

- #1 封面偶发不加载
- #9 文件夹刷新卡死、首次扫描慢
- #10 应用级扫描进度

依赖：后台任务模型、封面缓存策略、轻量索引格式。

### B. 单曲编辑与导航

- #2 重设计编辑页面并加入在线搜索/写入流程
- #5 移除歌曲页右上角编辑标签按钮
- #6 所有单曲 Item 点击打开编辑视图
- #15 艺术家文本跳转指定艺术家
- #17 展示 ReplayGain 等标签；流派支持多值和手动输入

依赖：统一单曲入口、插件搜索服务、完整标签模型。

### C. 插件与批处理

- #3 “数据源”更名为“插件源”
- #4 “任务”改为“批处理”，对齐移动端能力
- #7 实现真实插件系统

依赖：插件运行时、数据库仓储、后台任务队列、完整标签读写。

### D. 浏览体验

- #8 表格列宽可调整并限制最小/最大值
- #14 艺术家详情支持单曲视图/专辑视图切换

依赖：统一可调整表格组件、集合详情模型。

### E. 后端与持久化

- #11 拆分 Rust 单文件
- #12 重构并强化数据库层；因 RocksDB 构建成本过高，按后续确认保留 SQLite
- #13 使用 INI/YAML/JSON 保存应用配置和便携设置，业务数据继续使用数据库
- #16 优先把数据放在安装目录，不可写时保持现状

依赖：数据库仓储、schema 迁移、配置文件原子写入、便携模式目录解析。

## 3. 目标架构

### 3.1 前端

```text
src/
├─ app/                       # 应用装配、路由、全局任务状态
├─ domain/
│  ├─ library/               # Song/Album/Artist 领域模型与聚合
│  ├─ plugins/               # manifest、能力、搜索结果、字段策略
│  └─ batch/                 # 任务、任务项、状态、配置
├─ services/
│  ├─ libraryService.ts      # Tauri API 的领域包装
│  ├─ pluginService.ts
│  └─ batchService.ts
├─ stores/
│  ├─ libraryStore.ts
│  ├─ editorStore.ts
│  └─ taskStore.ts           # 监听全局进度事件
├─ features/
│  ├─ editor/
│  ├─ library/
│  ├─ plugins/
│  └─ batch/
└─ components/               # 无业务状态的复用组件
```

页面只负责渲染和路由；搜索、保存、扫描、插件执行和任务状态由 service/store 负责。

### 3.2 Rust

```text
src-tauri/src/
├─ lib.rs                    # Tauri 装配与 command 注册
├─ error.rs                  # 统一可序列化错误
├─ paths.rs                  # portable/fallback 数据目录
├─ models/                   # Song、Folder、Plugin、BatchTask
├─ commands/                 # 薄 command 层
│  ├─ library.rs
│  ├─ tags.rs
│  ├─ plugins.rs
│  └─ batch.rs
├─ library/
│  ├─ scanner.rs             # 枚举、并发读取、增量判断
│  ├─ index.rs
│  └─ artwork.rs
├─ tags/                     # lofty 读写和字段映射
├─ database/
│  ├─ mod.rs                 # 数据库连接和事务边界
│  ├─ schema.rs              # SQLite schema、索引和 user_version 迁移
│  ├─ repositories/          # 媒体库、插件、任务、日志仓储
│  └─ migration.rs           # 旧 AppData 数据库和配置迁移
├─ config/
│  └─ json_store.rs          # 应用配置的原子 JSON 写入
├─ plugins/
│  ├─ manifest.rs
│  ├─ installer.rs
│  ├─ manager.rs
│  ├─ runtime.rs
│  └─ host_api/
└─ batch/
   ├─ manager.rs
   ├─ worker.rs
   └─ processors/
```

## 4. 数据库与配置持久化设计

### 4.1 数据库选型

保留 **SQLite** 作为嵌入式数据库，但重建其使用方式。SurrealDB + RocksDB 的验证分支因首次编译耗时约 3.5 分钟、Windows 构建依赖 libclang/NASM 而放弃；该成本与当前桌面应用规模不匹配。

强化后的 SQLite 方案：

- 数据访问统一经过 Repository，不允许 Tauri command 或页面直接拼查询。
- 启用 WAL、外键、busy timeout、NORMAL synchronous 和内存临时表。
- 扫描写入、文件夹删除、标签更新和关系重建使用显式事务。
- 使用 `PRAGMA user_version` 管理 schema migration，而不是在启动时散落执行 `ALTER TABLE`。
- 为文件指纹、文件夹、专辑排序、艺术家排序、任务状态和日志建立索引。
- 保留独立的歌曲—艺术家、歌曲—专辑关系表，为后续多艺术家导航和集合查询提供基础。

数据库包含以下表：

| 表/关系 | 职责 | 主要索引 |
|---|---|---|
| `library_folder` | 文件夹、扫描状态、进度摘要 | path unique、status |
| `song` | 轻量标签摘要、文件指纹、技术信息 | path unique、folder、album key、mtime |
| `artist` / `album` | 聚合实体 | normalized name/key unique |
| `artist_song` / `album_song` | 多艺术家和专辑成员关系 | 双向 relation index |
| `source_plugin` | 插件身份、版本、启用和顺序 | plugin id unique、enabled/order |
| `plugin_setting` / `plugin_cache` | 插件配置与隔离缓存 | plugin id、cache key、expires at |
| `batch_task` / `batch_task_item` | 批处理和扫描任务 | status、created at、task relation |
| `app_log` | 插件、扫描、批处理诊断 | type、level、created at、related id |
| `PRAGMA user_version` | 数据库 schema 版本 | 单调递增版本号 |

数据库不保存完整 base64 封面；歌词正文只在确实需要搜索/预览缓存时保存，并带文件指纹，文件变化后失效。标签写入成功后必须重新读取音频文件，再更新数据库摘要。

### 4.2 JSON 配置

JSON 不替代数据库，只保存适合人工备份、迁移和便携运行的应用配置。相较 INI，JSON 能无损表达数组和嵌套配置，并直接映射 serde/TypeScript 类型。

```text
data/
├─ config/
│  ├─ settings.json          # 语言、列宽、视图偏好、扫描并发等
│  └─ data-location.json     # 便携模式及实际数据库目录
├─ database/
│  └─ lyrico.sqlite3         # 强化 schema、WAL 和事务的主数据库
├─ plugins/
│  └─ sources/<plugin-id>/   # manifest.json、脚本、图标；状态和配置存数据库
└─ logs/
```

JSON 配置写入规则：

- 所有文件含 `schemaVersion`。
- 先写同目录临时文件，flush 后原子替换；保留最近一个 `.bak`。
- 进程内用读写锁串行化同一文件更新。
- 文件损坏时读取 `.bak`，仍失败则报告可见错误，不能静默清空。
- 旧 AppData 根目录中的 `lyrico.sqlite3` 在新安装目录数据库不存在时复制迁移；随后通过 `user_version` 增量升级 schema。

数据目录策略：

1. 可执行文件旁存在 `portable.flag` 或 `data/` 且可写时，使用安装目录旁 `data/`。
2. 否则尝试创建并写入安装目录旁 `data/`。
3. 无写权限时回退 Tauri `app_data_dir`，并在设置页显示实际路径和回退原因。

## 5. 分阶段实施

### Phase 0：基线与回归保护

- [x] 核对当前真实技术栈和代码入口。
- [x] 运行现有 `npm test`、`npm run build`、`cargo check`。
- [ ] 为封面缓存、路径归属、艺术家拆分、数据库仓储、JSON 配置原子写入和扫描增量判断补测试。
- [ ] 建立统一错误模型和前端错误提示入口。

验收：改造前测试基线有记录；后续每阶段不降低通过率。

### Phase 1：低耦合交互与封面稳定性

- [x] 封面请求失败不再永久缓存为“无封面”；批量请求支持去重、有限退避重试和刷新失效。
- [x] 编辑抽屉移动到应用级，所有 `LibraryTable` 单曲行使用同一 `openTrackEditor(path)`。
- [x] 移除歌曲页右上角“编辑标签”按钮，以实际点击的行作为编辑目标。
- [x] 专辑、艺术家、文件夹详情中的单曲行点击打开编辑视图。
- [x] 从专辑/艺术家单曲 Drawer 打开编辑器时先关闭集合 Drawer，避免编辑视图被压在下层。
- [x] 取消歌曲、专辑、艺术家和批处理表格分页，避免翻页切断加载与选择状态。
- [x] 封面改为视口前 240px 预取、同帧请求合并、每批最多 32 个路径；失败不写入负缓存并保留退避重试。
- [x] 替换 WebView 默认右键菜单：编辑控件提供撤销/重做/剪切/复制/粘贴/全选，普通区域提供应用导航。

验收：模拟一次封面读取失败后无需刷新应用即可恢复；任何列表点击的路径与编辑器显示/保存路径一致。

### Phase 2：Rust 拆分与数据库迁移

- [x] 抽取 `models/audio/database/config/commands/paths`，保持 command 名称兼容。
- [x] 建立独立 SQLite Repository，启用 WAL、事务、外键、busy timeout 和 schema version。
- [x] 建立媒体库关系、插件、插件缓存、批处理、任务项和日志表及索引。
- [x] 艺术家拆分设置迁移到 JSON，业务索引、插件、任务和日志保留在数据库。
- [x] 歌曲表只读取轻量摘要和文件指纹；扫描不再生成或持久化新的 base64 封面。
- [x] 实现旧 AppData 数据库和旧数据库设置的一次性兼容迁移。
- [x] 实现安装目录优先、不可写回退 AppData，并在设置页展示实际数据库位置。

验收：全新安装、旧库迁移、事务回滚、schema 升级、JSON 配置损坏回退、只读安装目录六种场景均通过；数据库仓储测试必须验证外键和版本设置。

### Phase 3：后台扫描与应用级进度

- [x] `scan_folder` 将阻塞工作放入后台线程，并通过全局事件提供稳定 `jobId`；页面可在等待结果时正常切换。
- [x] 扫描分为文件枚举、标签读取、索引提交三个阶段。
- [x] 使用最多 4 线程的有界 Rayon 工作池并发读标签，避免无界线程和内存增长。
- [x] 根据 `path + size + mtime + scan signature` 跳过未变化文件；删除项在本轮事务提交时清理。
- [x] 同一文件夹重复刷新会被扫描注册表拒绝，禁止并发互相覆盖。
- [x] 通过 Tauri event 推送 `running/completed/failed` 和阶段、数量、错误数。
- [x] 应用 Shell 常驻显示扫描进度，切换页面不丢失。
- [ ] 增加扫描取消命令，并在文件夹页展示可展开的失败文件明细。

验收：刷新时 UI 可持续操作和切页；大目录二次扫描明显快于首次扫描；任务可取消；单文件失败不终止整个目录。

### Phase 4：真实插件系统（移动端 API v3）

优先级调整（2026-07-13）：暂停继续扩展批处理器，先完成插件系统；标签匹配、在线歌词和在线封面均依赖此阶段，不再使用前端内存中的演示插件冒充实现。

桌面端严格复用移动端协议与生命周期：

1. `manifest.json` 使用 API v3，字段为 `id/name/versionCode/versionName/apiVersion/minHostApiVersion/entry/includeDirs/icon/capabilities/configFields`。
2. ZIP 导入执行路径穿越、NUL、反斜杠、深度、文件数、解压后总大小、manifest/入口/插件大小限制；递归发现多个 manifest。
3. 安装使用同一插件根目录内的 staging/backup 原子替换，首次默认禁用，更新保留 enabled、sortOrder 和用户配置。
4. `includeDirs` 中 `.js` 按相对路径排序后加载，再加载入口；每插件独占 QuickJS 运行时，64 MB 内存、2 MB 栈、15 秒默认超时。
5. Host API 与移动端 `Platform` 对齐，不把 Node/Tauri/WebView 对象暴露给插件；缓存、HTTP、加密、Base64、压缩、XML、日志按插件隔离。
6. 运行结果只接受标准 `fields/internal` 模型，字段写入策略由宿主管理。

- [ ] 支持 ZIP 导入、manifest 递归发现、安装/更新/覆盖/降级判断；真实安装入口已接通，尚需在多插件包或覆盖安装前增加候选确认 UI。
- [x] 对齐移动端校验：反向域名 ID、API v3、能力、入口/include 路径、大小、文件数、ZIP 穿越防护。
- [x] 插件原子安装到 `plugins/sources/<id>`，默认禁用；更新保留启用状态、顺序和已有配置。
- [x] 实现 manifest `configFields` 的 text/password/number/switch/dropdown/textarea/markdown 和 `match/and/or/not` 依赖表达式；Markdown 使用安全渲染而非纯文本，配置与 manifest 恢复为 Tab 切换。
- [x] `defaultValue` 运行时统一为字符串，但安装解析兼容插件包中的 string/boolean/number/null 标量；布尔与数字规范化为字符串，数组和对象继续拒绝，避免 QQ 插件的布尔 switch 默认值导致整包无法安装。
- [x] 实现隔离 QuickJS、同插件串行执行、15 秒超时、64 MB 内存和 2 MB 栈限制；每次调用创建干净上下文，持久状态只能使用隔离 cache。
- [ ] 第一轮 Host API 已覆盖官方插件搜索/多数歌词所需的 app/runtime/cache/http/log/base64/crypto/compression；XML 四项仍需补齐以实现 Apple TTML 官方本地化完全对齐。
- [x] 实现 `searchSongs/getLyrics/searchCovers` 调用、宽松结果列表解析、插件私有 `internal` 原样回传；编辑页按用户操作应用已有标准字段，缺失字段不清空本地值。
- [ ] 插件日志、权限调用和错误进入诊断页，不再使用静态演示插件。
- [x] 将导航和页面文案从“数据源”统一改为“插件源”。
- [x] 插件图标由 Rust 校验读取后以 Data URL 传给前端，避免 WebView 本地资源权限导致图标丢失；插件列表为图标、文本、状态预留独立布局间距。
- [x] 插件详情的卸载操作与启停开关统一放入标题操作区；左右卡片取消等高拉伸，各自按内容决定高度。

2026-07-13 验证证据：`cargo check` 通过；插件安装器/运行时测试 4 项通过；直接加载移动端 `Lyrico-Plugins/kugou` 包的联网 `searchSongs` 兼容测试通过；`npm run build` 通过。网易云实包在当前网络下 EAPI 与 fallback 均返回空数组，已记录为后续跨宿主对照项，不能据此判定运行时兼容完成。

验收：直接安装移动端仓库中的 Apple/网易云/QQ/酷狗插件，能启用、配置、搜索、取歌词/封面、禁用和卸载；故障插件不能拖死主进程。

### Phase 5：编辑器重设计与完整标签模型

编辑器改为三部分：

1. **文件与媒体摘要**：封面、文件名、路径、格式、时长、码率；信息按组排列，不再拥挤在封面右侧。
2. **本地标签**：基础字段、贡献者字段、歌词、注释、ReplayGain、自定义字段。
3. **在线匹配**：插件源顺序、搜索词、候选结果、字段差异、逐字段应用/全部应用。

具体改动：

- [ ] 按移动端 `EditFieldRegistry` 固定六组及字段类型：基础信息（文本/多值流派）、曲目详情（曲目号/碟号）、贡献者与其他（作曲/作词/版权/单行注释）、ReplayGain（数值文本）、歌词（多行文本）、封面（图片/整数评分）；歌词偏移仅在批处理出现。
- [x] 编辑器第一轮布局已按上述六组重建；本地标签和在线匹配分区，文件技术信息独立展示；在线区已接入插件选择、标签搜索、封面搜索、字段应用、在线封面下载和歌词获取。

- [x] “备注”统一改为“注释”，使用真实单行输入；长内容需要时显式展开编辑。
- [x] 移除“歌词已找到”“封面已内嵌”两个无效状态项。
- [x] 文件路径、标签摘要和技术信息移到独立“文件信息”Tab，不再固定堆叠在编辑页底部。
- [ ] 展示并可编辑 Track/Album Gain、Track/Album Peak、Reference Loudness；前四项已接通通用标签读写，Reference Loudness 等待格式专用适配器后解除只读。
- [ ] 标签模型补齐 composer、lyricist、copyright、rating、language、customFields；除 customFields 外均已接入第一轮读写。
- [x] 流派使用 tags 模式：支持多选、手动输入、粘贴分隔、去重；Lofty 写入多值标签并由格式层执行兼容映射。
- [ ] 封面支持替换、移除、还原和来自插件候选的预览；本地图片替换、移除、还原、复用同专辑封面及导出当前编辑态封面已接通，在线搜索和裁剪等待插件/图片工具阶段。
- [x] 歌词本地操作第一阶段：UTF-8 文件导入；TTML 导出 `.ttml`、其他歌词导出 `.lrc`；LRC/Enhanced LRC/TTML 时间轴按 `-500/-100/+100/+500 ms` 偏移；移除空行和普通文本查看。所有操作只更新编辑器临时内容。
- [ ] 移植移动端 `LyricsDocumentPipeline` 的结构化处理：格式识别与转换、逐句排序、非歌词内容过滤、原文/翻译/罗马音轨道策略，以及简繁转换；不得用正则拼接冒充逐字时间轴转换。
  - [x] 第一阶段已将插件歌词转换从正则拼接替换为统一 `LyricsDocument`：LRC 元数据、原文/翻译/罗马音/和声轨道、TTML `itunes:key`、演唱者和多种时间表达均先解析再写出；同格式无处理要求时保留原始文本，避免破坏插件扩展。
  - [x] 修复无显式行结束时间时吞掉最后一个逐字歌词、TTML 秒数时间解析失败、按数组下标错配翻译，以及逐字之间无时间空格丢失；新增对应往返回归测试。
  - [ ] 后续继续对齐移动端的逐句排序、非歌词内容过滤、简繁转换和完整未知 TTML 扩展透传。
- [x] 在线歌词与在线封面通过真实插件系统接入编辑器临时状态，不直接写文件；结构化歌词可按移动端四种目标格式（普通 LRC、逐字 LRC、增强 LRC、TTML）预览和转换后再确认。
- [x] 插件搜索结果保留“查看标签 / 查看封面 / 查看歌词”三个独立动作：标签支持选择字段并按覆盖/补充模式应用，封面只允许选择原图或目标最大尺寸而不修改来源 URL，歌词支持格式切换和文本微调；各自确认后才写入编辑器临时状态并切回本地标签。
- [x] 删除在线匹配顶部重复的“搜索封面”入口；一次歌曲搜索同时承载标签、封面和歌词候选，结果行继续保留三个独立查看动作。
- [x] 标签应用对话框第三轮对齐移动端：每行仅展示一个标签字段，字段行内包含启用勾选和覆盖/补充策略；顶部提供全选/取消全选和批量策略切换，批量操作只修改行策略而不绕过逐字段选择。
- [x] 在线匹配默认并发查询全部已启用歌曲搜索插件；结果使用“全部 + 各插件源”Tab，全部 Tab 标注来源，单源失败不得清空其他源结果，搜索数量读取持久化设置。
- [x] QQ 源空结果已用插件仓库 devkit 建立对照：devkit 返回 5 条结果，而 Rust Host API 收到 `code=500001`；根因是 bootstrap 携带的 `bodyBytes: null` 被 Rust 误判为二进制请求并覆盖正常 JSON body，现仅在 `bodyBytes` 为数组时启用该分支，并保留真实 QQ 插件网络集成测试用于复验。
- [x] 编辑器顶部封面右侧只展示替换、同专辑、导出、移除和还原操作；歌曲文本及音频参数统一进入文件信息 Tab。
- [x] 文件信息 Tab 不再合并展示“音频参数”字符串；格式、时长、比特率、采样率和声道数分别使用独立字段展示。
- [ ] 保存前展示变更摘要，保存后只刷新对应文件和受影响的专辑/艺术家聚合。
- [x] 修复折叠字段为空或尚未挂载时命令参数缺失：前端提交完整快照，显式空值表示删除，未挂载字段保留文件原值；Rust 不把缺失字段擅自解释为空值。
- [ ] 在格式兼容测试完成后再决定是否由 Lofty 切换到移动端同款 TagLib；参数反序列化错误与标签库无关，不能通过换库掩盖协议问题。

验收：本地编辑和插件匹配都通过同一保存管线；不支持多值的格式有明确降级规则；保存后重新从文件读取核验。

### Phase 6：统一单曲/集合导航

- [ ] 提取统一 `TrackItem`/`LibraryTable` 交互契约，点击永远打开对应路径编辑器。
- [ ] 艺术家详情增加“单曲/专辑”分段切换；专辑视图按同一 ArtistGroup 下的 AlbumGroup 聚合。
- [ ] 歌曲详情和专辑详情中的艺术家文本可点击。
- [ ] 使用移动端同一艺术家拆分规则；多艺术家先弹出选择列表，单艺术家直接跳转。
- [ ] 跳转时切换到艺术家 Tab、设置目标 artistId、打开对应详情，并保留返回上下文。
- [x] 建立应用级多选会话；歌曲页、专辑详情、艺术家详情和文件夹歌曲列表均可直接进入/退出多选，点击整行切换选择并一键进入批处理。
- [x] 专辑和艺术家主列表增加与歌曲页一致的多选入口；勾选一个专辑/艺术家时向应用级选集加入其全部歌曲，取消时移除该组歌曲，同时保留详情单曲表的逐首选择能力。
- [x] 歌曲、专辑和艺术家主页面统一列表页骨架：多选入口固定在页面标题栏右上角，Card 顶部显示当前列表名称和总数，选择模式下使用同一批处理操作条。
- [x] 侧边栏增加常驻“已选歌曲”入口；入口位于折叠按钮上方，数量内嵌以避免侧栏边界裁剪，展开/收起状态使用同一纵向操作区。轻量抽屉只展示标题、艺术家和移除操作，并提供清空选集与直接进入批处理，数据直接读取应用级选集而不维护副本。

验收：歌曲、专辑、艺术家、文件夹、批处理结果中的单曲都打开正确文件；多艺术家选择不会误跳。

### Phase 7：表格列宽

- [ ] 建立统一列定义和列宽状态，按表格类型保存到 `config/settings.json`。
- [x] 表头拖拽调整宽度；歌曲、专辑、艺术家和文件夹主表的标题及数值列已定义 min/max/default。
- [ ] 拖拽过程使用局部状态，结束后再持久化，避免频繁写盘。
- [ ] 窄窗口继续支持横向滚动；提供“恢复默认列宽”。

验收：所有主要表格可调整；宽度不会小于最小值或超过最大值；重启后保留。

### Phase 8：批处理中心

- [ ] 导航“任务”改为“批处理”，提供“新建任务”和“任务历史”。
- [x] 批处理工作台第二轮减法重构：删除说明副标题、空历史 Tab、选择 Alert、0% 进度、统计卡和未实现占位组件；选集来自任意歌曲 Item 列表，页面只保留选中数量、动作栏和当前动作内容，未接通动作禁用而非展示假页面。
- [ ] 配置区按处理器提供参数、影响字段和执行前差异预览；任务历史与新建流程分离，禁止用多个功能标签页替代任务流程。
- [ ] 实现任务/任务项状态：queued/running/succeeded/failed/skipped/cancelled；已接通 queued 任务创建、任务/任务项状态迁移、计数聚合、取消与任务列表/明细 API。ReplayGain 已成为第一条真实执行链路，其他处理器待逐项接入。
- [ ] 实现并发限制、取消、单项错误隔离、结果日志和失败项重试。
- [ ] 对齐移动端处理器：
  - 标签匹配：插件顺序、字段补充/覆盖/禁用、文件名优先、并发 1–5。
  - 批量编辑：`<keep>` 语义、预览、封面与自定义字段。
  - 重命名：`@1`–`@8`、非法字符映射、冲突自动编号、执行前预览。
  - 歌词格式化：目标格式、逐句排序、过滤非歌词、移除空行。
  - 导出歌词：TTML 为 `.ttml`，其余 `.lrc`。
  - 导出封面：按原文件基名导出，保留/转换策略明确。
  - ReplayGain：已有值跳过；桌面端现已从批处理页触发 ebur128 分析并写入 track gain/peak，任务级成功/跳过/失败/取消已记录；reference loudness 等待格式专用适配器。
- [ ] ReplayGain 计算严格对齐移动端 ebur128 链路：
  - [x] 桌面端使用 Symphonia 流式解码到交错 PCM，样本送入 `ebur128`（`libebur128` 的 Rust 移植版，官方声明结果一致且通过 EBU TECH 3341/3342 测试），不得用 RMS 或标签估算替代。
  - [x] 已实现单曲后台分析命令、限频进度事件、跨页面 Shell 进度、取消，以及分析结果只回填编辑器临时状态；Rust 最多每 100 ms 推送一次运行进度，前端最多每 160 ms 刷新一次独立进度存储，终态立即显示。Shell/单曲编辑器局部订阅该存储，批处理表格和应用根组件不再随每个进度事件重渲染。
  - [x] 已使用移动端 TagLib 测试集中的真实 FLAC 样本验证完整解码和响度分析。
  - [ ] 实现专辑多状态分析命令并接入专辑页与批处理器。
  - [ ] 将当前由前端串行驱动的批处理编排迁移到 Rust 后台任务运行器，并按移动端提供 1–5 的有界并发（默认 3）；页面仅订阅任务快照，关闭或切换页面不影响任务。
  - 曲目响度使用 integrated/global loudness，峰值使用 true peak；目标响度固定为 `-18 LUFS`。
  - `gain = -18 - measured_loudness`，Gain 使用 `%.2f dB`，Peak 使用 `%.6f` 且仅限制最小值为 0。
  - 专辑模式保留每首曲目的 ebur128 state，使用 `ebur128_loudness_global_multiple` 聚合专辑响度，专辑峰值取曲目 true peak 最大值，禁止直接平均 LUFS。
  - 单曲编辑允许取消和覆盖重算；批处理只要任一 ReplayGain 字段已存在就跳过该文件，并记录 skipped 原因。
  - 分析结果先进入编辑器临时状态；只有保存或批处理执行阶段才写文件，写入后重新读取文件核验。
- [ ] 建立 ReplayGain 格式适配器，分别处理 ID3v2、Vorbis Comment、APE 和 MP4 的 `REPLAYGAIN_REFERENCE_LOUDNESS`；Lofty 通用 `ItemKey` 不支持该键时不得静默丢弃。
- [ ] 批处理进度与扫描任务共用 Shell 全局任务中心。

验收：任务切页和重启后历史仍在；每项成功/失败/跳过可追溯；取消不会继续写后续文件。

### Phase 9：有效设置中心

- [x] 删除仅用于展示产品/框架信息、静态摘要或重复导航的占位设置页签，改为与媒体库页面一致的标题、说明和内容卡布局。
- [x] 设置页改为左侧分类导航与单一内容区；删除插件名称堆叠、音乐库计数、持久化路径和重复管理入口，只保留界面、在线匹配、歌词与音乐库中真正可修改的设置。
- [x] `settings.json` 增加版本化桌面设置模型，至少持久化在线搜索结果数量、默认歌词格式、是否写入翻译、是否写入罗马音、仅翻译策略和移除空行；未知字段向前兼容，写入继续使用临时文件和备份。
- [x] 在线匹配与歌词预览必须直接读取上述设置：搜索请求的 `pageSize`、默认歌词格式及原文/翻译/罗马音编码行为不得停留为无效 UI。
- [x] 保留真正有操作结果的语言、艺术家拆分、文件夹管理和插件管理入口；插件自身配置仍由插件源页面负责，不在设置页复制一套状态。

验收：重启后设置仍存在；修改搜索数量会改变各插件请求；关闭翻译或罗马音后在线歌词预览不再编码对应轨道。

## 6. 需求—阶段映射

| 需求 | 阶段 |
|---|---|
| 1 | Phase 1 |
| 2 | Phase 5 |
| 3 | Phase 4 |
| 4 | Phase 8 |
| 5 | Phase 1 |
| 6 | Phase 1、6 |
| 7 | Phase 4 |
| 8 | Phase 7 |
| 9 | Phase 2、3 |
| 10 | Phase 3 |
| 11 | Phase 2 |
| 12 | Phase 2 |
| 13 | Phase 2 |
| 14 | Phase 6 |
| 15 | Phase 6 |
| 16 | Phase 2 |
| 17 | Phase 5、8 |
| 补充 1：批处理布局与逻辑重设计 | Phase 8 |
| 补充 2：替换浏览器默认右键菜单 | Phase 1 |
| 补充 3：编辑字段、分类和类型对齐移动端 | Phase 5 |
| 补充 4：取消分页 | Phase 1、7 |
| 补充 5：封面提前加载与其他项目方案对比 | Phase 1 |
| Bug：集合单曲 Drawer 遮挡编辑 Drawer | Phase 1 |
| Bug：空字段/未挂载字段导致保存参数缺失 | Phase 5 |
| 补充：任意歌曲列表进入多选并跳转批处理 | Phase 6、8 |
| 补充：侧边栏查看、移除和清空已选歌曲 | Phase 6、8 |
| 补充：单曲编辑器封面替换/移除/还原/同专辑封面 | Phase 5 |
| 补充：歌词导入导出、偏移、格式化与文本查看 | Phase 5、8 |

## 7. 每阶段统一验证

```powershell
npm test
npm run build
cargo test --manifest-path src-tauri/Cargo.toml
cargo check --manifest-path src-tauri/Cargo.toml
```

涉及扫描、插件、写标签或文件重命名时，额外使用测试音乐副本验证，不在唯一原件上做破坏性测试。
